export interface ThemeConfig {
  colors: {
    primary: string;
    secondary: string;
    background: string;
    foreground: string;
    accent: string;
  };
  fonts: {
    sans: string;
    mono: string;
  };
} 